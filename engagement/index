<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>We’re Engaged</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Our engagement announcement — date and photos." />

  <!-- Social share (optional) -->
  <meta property="og:title" content="We’re Engaged">
  <meta property="og:description" content="Save the date and browse our favourite photos.">
  <meta property="og:type" content="website">
  <meta property="og:image" content="/engagement/og-image.jpg"> <!-- replace if you like -->

  <style>
    /* Ultra-light, mobile-first, no framework */
    :root{
      --bg:#fafafa; --card:#fff; --ink:#111; --muted:#666; --accent:#0b5fff;
      --max:1024px; --pad:clamp(16px,3vw,28px);
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0;background:var(--bg);color:var(--ink);font:16px/1.55 system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif}
    a{color:var(--accent);text-decoration:none}
    .wrap{max-width:var(--max);margin:0 auto;padding:var(--pad)}
    .hero{
      min-height:100svh; display:grid; place-items:center; position:relative; overflow:hidden; background:#000;
    }
    .hero img{
      position:absolute; inset:0; width:100%; height:100%; object-fit:cover; filter:brightness(.8);
    }
    .hero .content{
      position:relative; z-index:1; text-align:center; color:#fff; padding:clamp(16px,4vw,32px);
      backdrop-filter: blur(2px);
    }
    h1{margin:0 0 .25em; font-size:clamp(32px,6vw,56px); letter-spacing:.5px}
    .date{
      display:inline-block; margin-top:.25em; padding:.4em .8em; border-radius:999px;
      background:rgba(255,255,255,.15); color:#fff; font-weight:600; font-size:clamp(14px,2.8vw,18px)
    }
    .lead{margin:.8em auto 0; max-width:42ch; color:#f0f0f0}

    /* Section headings */
    .section{padding:clamp(28px,6vw,56px) var(--pad)}
    .section h2{margin:0 0 .5em; font-size:clamp(22px,4.5vw,32px)}

    /* Masonry-like gallery using CSS columns (simple + fast) */
    .gallery{
      columns: 1; column-gap:12px;
    }
    @media (min-width: 560px){ .gallery{ columns: 2; } }
    @media (min-width: 900px){ .gallery{ columns: 3; } }

    .card{
      break-inside: avoid; margin:0 0 12px; background:var(--card); border-radius:14px; overflow:hidden;
      box-shadow: 0 8px 24px rgba(0,0,0,.08);
    }
    .card img{ display:block; width:100%; height:auto; }
    .caption{ padding:12px 14px; color:var(--muted); font-size:.95rem }

    /* Footer */
    .footer{ text-align:center; color:var(--muted); padding:24px var(--pad) 40px }

    /* Reduce motion for accessibility */
    @media (prefers-reduced-motion: no-preference){
      .card{ transition: transform .15s ease }
      .card:hover{ transform: translateY(-2px) }
    }
  </style>
</head>
<body>

  <!-- HERO -->
  <header class="hero">
    <!-- Replace hero.jpg with your image (2000px+ wide recommended). Use AVIF/WebP if possible. -->
    <img src="/engagement/hero.jpg" alt="Us" fetchpriority="high">
    <div class="content">
      <h1>We’re Engaged 🎉</h1>
      <!-- Edit the date text; keep it short -->
      <div class="date">Wedding: 12 July 2026</div>
      <p class="lead">Thank you for sharing our joy. Scroll for a few favourite moments.</p>
    </div>
  </header>

  <!-- GALLERY -->
  <main class="section wrap" aria-label="Engagement Photos">
    <h2>Photos</h2>
    <div class="gallery">

      <!-- Each .card = one photo. Replace src/srcset/alt. Keep loading="lazy" and width/height for CLS. -->
      <figure class="card">
        <img
          src="/engagement/img1-1200.jpg"
          srcset="/engagement/img1-800.jpg 800w, /engagement/img1-1200.jpg 1200w, /engagement/img1-1600.jpg 1600w"
          sizes="(max-width: 560px) 100vw, (max-width: 900px) 50vw, 33vw"
          alt="At the lookout"
          loading="lazy" width="1200" height="1600">
        <figcaption class="caption">The spot.</figcaption>
      </figure>

      <figure class="card">
        <img
          src="/engagement/img2-1200.jpg"
          srcset="/engagement/img2-800.jpg 800w, /engagement/img2-1200.jpg 1200w, /engagement/img2-1600.jpg 1600w"
          sizes="(max-width: 560px) 100vw, (max-width: 900px) 50vw, 33vw"
          alt="Laughing mid-blooper"
          loading="lazy" width="1200" height="1500">
        <figcaption class="caption">Blooper reel energy.</figcaption>
      </figure>

      <figure class="card">
        <img
          src="/engagement/img3-1200.jpg"
          srcset="/engagement/img3-800.jpg 800w, /engagement/img3-1200.jpg 1200w, /engagement/img3-1600.jpg 1600w"
          sizes="(max-width: 560px) 100vw, (max-width: 900px) 50vw, 33vw"
          alt="Close-up of the ring"
          loading="lazy" width="1200" height="1200">
        <figcaption class="caption">The ring ✨</figcaption>
      </figure>

      <!-- Duplicate the block above for more photos (img4, img5...) -->

    </div>
  </main>

  <footer class="footer wrap">
    <small>© <span id="y"></span> With love.</small>
  </footer>

  <script>
    // Set year without extra libraries
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>
</body>
</html>
